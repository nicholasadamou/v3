import React from 'react'

import CircularProgress from '@material-ui/core/CircularProgress'

const Loading = () => (
		<CircularProgress style={{ width: 10, height: 10, color: '#363636' }} />
);

export default Loading
