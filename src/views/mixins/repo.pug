mixin repo(link, emoji, title, desc)
	article.repo
		a(href=link target="_blank" aria-hidden="true" rel="noopener")
			.top
				.left
					span.emoji #{emoji}
					span.repo-title #{title}
				.right
					span.stars ⭐️
		.bottom
			span.desc #{desc}
