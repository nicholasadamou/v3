mixin repo(link, emoji, title, desc)
	article.repo
		a(href=link aria-hidden="true")
			.top
				.left
					span.emoji #{emoji}
					span.repo-title #{title}
				.right
					span.stars ⭐️
		.bottom
			span.desc #{desc}
