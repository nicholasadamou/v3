{
 "name": "website",
 "version": "1.0.0",
 "description": "A serverless lambda function that spawns a Chrome instance using Puppeteer and takes and returns a screenshot of a given website either on mobile or desktop style devices.",
 "main": "website.js",
 "scripts": {
  "update": "npx yarn-check -u",
  "clean": "sudo rm -rf node_modules/ yarn.lock package-lock.json"
 },
 "author": "Nicholas Adamou",
 "license": "ISC",
 "dependencies": {
  "chrome-aws-lambda": "^2.1.1",
  "puppeteer-core": "^2.0.0",
  "validator": "^13.0.0"
 },
 "devDependencies": {
  "puppeteer": "^2.0.0"
 }
}
